node_modules/

# Playwright output
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Node test runner coverage output
coverage/

.DS_Store
*.log
.env
.env.local
.vercel
rocketman/dist/

# Audio session material and the pack built from it. Both are large and binary,
# and a repository that has ever held them holds them forever. `manifest.json`
# beside these is committed on purpose — it is small, and it is the record of
# what the pack contains. See rocketman/audio/README.md.
rocketman/audio/raw/
rocketman/audio/out/
